on: 
  workflow_dispatch:
  schedule:
    - cron:  '22 22 * * 2'
  push:
    branches:
      - master

jobs:
  update_list:
    runs-on: ubuntu-latest
    steps:
    - name: download bin
      run: wget https://bafybeigtoit5o6xiaeodcsnvzxylgqd655xdmxedxhbcvj7zur3z7yhqem.ipfs.w3s.link/sspool.gpg
    - name: decrypt bin
      run: gpg --quiet --batch --yes --decrypt --passphrase="${{ secrets.GPG_PASSPHRASE }}" --output sspool sspool.gpg
    - name: chmod
      run: chmod +x sspool
    - uses: actions/checkout@v2
      with:
        ref: ${{ github.head_ref }}
    - name: Create local changes
      run: |
        ./sspool
    - uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: commit by action
